theorem complex_power_example : (1 + I)^10 = 32 * I := by
  have h1 : (1 + I) ^ 2 = 2 * I := by
    admit
  
  have h2 : (1 + I) ^ 4 = -4 := by
    admit
  
  have h3 : (1 + I) ^ 8 = 16 := by
    admit
  
  have h4 : (1 + I) ^ 10 = 32 * I := by
    admit
  
  admit
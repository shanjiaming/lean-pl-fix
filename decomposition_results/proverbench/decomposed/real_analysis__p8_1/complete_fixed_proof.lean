theorem positivity_of_square_plus_one (x : ℝ) : 0 < x^2 + 1 := by
  have h₁ : 0 ≤ x^2 := by
    
    admit
  
  have h₂ : 0 < x^2 + 1 := by
    
    have h₂₁ : 0 ≤ x^2 := h₁
    have h₂₂ : 0 < x^2 + 1 := by
      
      admit
    admit
  
  admit
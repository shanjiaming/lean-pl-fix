theorem sin_sqrt_x_over_sqrt_x_integral (x C : ℝ) : ∫ (x : ℝ), sin √x / √x = -2 * cos √x + C :=
  by
  have h1 : False := by sorry
  have h2 : ∫ (x : ℝ), sin (sqrt x) / sqrt x = -2 * cos (sqrt x) + C := by sorry
  --  exact h2
  hole
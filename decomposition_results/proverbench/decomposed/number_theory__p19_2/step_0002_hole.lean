theorem h_main  : ∃ S, S.Infinite ∧ ∀ p ∈ S, Nat.Prime p ∧ p ≡ 9 [MOD 10] := by
  --  classical
  --  by_contra!
  --  have h₁ := this ∅
  --  have h₂ := this (Set.univ)
  --  have h₃ := this {0}
  --  have h₄ := this { 1 }
  --  have h₅ := this { 2 }
  --  have h₆ := this { 3 }
  --  have h₇ := this { 4 }
  --  have h₈ := this { 5 }
  --  have h₉ := this { 6 }
  --  have h₁₀ := this { 7 }
  --  have h₁₁ := this { 8 }
  --  have h₁₂ := this { 9 }
  --  have h₁₃ := this { 10 }
  --  have h₁₄ := this { 11 }
  --  have h₁₅ := this { 12 }
  --  have h₁₆ := this { 13 }
  --  have h₁₇ := this { 14 }
  --  have h₁₈ := this { 15 }
  --  have h₁₉ := this { 16 }
  --  have h₂₀ := this { 17 }
  --  have h₂₁ := this { 18 }
  --  have h₂₂ := this { 19 }
  --  have h₂₃ := this { 20 }
  --  norm_num [Set.Infinite, Set.Finite, Set.subset_def,
  --      Nat.ModEq] at h₁ h₂ h₃ h₄ h₅ h₆ h₇ h₈ h₉ h₁₀ h₁₁ h₁₂ h₁₃ h₁₄ h₁₅ h₁₆ h₁₇ h₁₈ h₁₉ h₂₀ h₂₁ h₂₂ h₂₃ <;>
  --    aesop
  hole